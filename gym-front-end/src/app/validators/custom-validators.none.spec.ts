import { CustomValidators } from './custom-validators.none';

describe('CustomValidator', () => {
  it('should create an instance', () => {
    expect(new CustomValidators()).toBeTruthy();
  });
});
